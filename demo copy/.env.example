# Sao chép thành .env nếu cần ghi đè (không bắt buộc — mặc định đã dùng http://localhost:8082 trong apiClient)
VITE_API_BASE_URL=http://localhost:8082

# Tuỳ chọn — ép parser (không đặt thì BE tự chọn: service nào bật thì gửi qua đó; cả hai bật thì theo BE exam-import.python-parser-default)
# VITE_EXAM_PARSER_ENGINE=legacy
